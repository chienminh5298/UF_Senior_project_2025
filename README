# UF Senior Project 2025 - Setup Guide

## Prerequisites
- Docker and Docker Compose installed
- Git installed

## Initial Setup

### 1. Clone the Repository
```bash
git clone <repository-url>
cd UF_Senior_project_2025
```

### 2. Start Docker Containers
**Notice**:Please don't start botTrade container because it's running a live trading on our project now !!! I comment out botTrade container by default

```bash
docker-compose -f docker-compose.dev.yml up -d
```

### 3. Database Setup
#### Make sure you have db.sql in folder backupDB

### 4. Add .env file in webServer folder
```bash
GOOGLE_APP_SCRIPT = ""
JWT_SECRET="your_jwt_secret_key_here"
ADMIN_USERNAME=ADMIN_USERNAME
ADMIN_PASSWORD=5hfj2bfoof7e23f
```

3. **Access the application:**
   - Frontend User: http://localhost:3000 (username: chienminh5298@gmail.com - password: test123)
   - Frontend Admin: http://localhost:3002
**Notice**: We can't share Goole Sheet, Goole App Script, AWS Lambda,... link but we can share code but you don't need these things to start application

## Common Issues

### Issue: "The table does not exist in the current database"
**Solution**: Make sure you have db.sql in folder backupDB

### Issue: API returns 500 errors after migration
**Solution**: Restart the backend service:
```bash
docker-compose -f docker-compose.dev.yml restart backend
```

## Development Commands

### View Logs
```bash
# All services
docker-compose -f docker-compose.dev.yml logs -f

# Specific service (backend, frontend, database)
docker-compose -f docker-compose.dev.yml logs -f backend
```

### Stop All Services
```bash
docker-compose -f docker-compose.dev.yml down
```

### Rebuild Containers (after dependency changes)
```bash
docker-compose -f docker-compose.dev.yml up -d --build
```

### Access Database Shell
```bash
docker-compose -f docker-compose.dev.yml exec database mysql -u root -p moneymachine
# Default password is usually defined in docker-compose.dev.yml
```

## Project Structure
- `/webServer` - Backend API (Node.js/Express/Prisma)
- `/frontendUser` - User-facing frontend (React)
- `/frontendAdmin` - Admin dashboard (React)
- `/botTrade` - Trading bot service
- `/backupDB` - Database backup files
- `/historical_data` - Historical trading data for backtesting

## Additional Resources
- Prisma Documentation: https://www.prisma.io/docs
- Docker Documentation: https://docs.docker.com
